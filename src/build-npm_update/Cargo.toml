# Autogenerated by gen-cargo-toml (but checked in)
[build-dependencies]
[build-dependencies.build_help]
path = "../build-help"
version = "=0.0.0"

[dependencies]

[features]
use_llvm = ["build_help/use_llvm"]

[lib]
path = "lib.rs"

[package]
build = "build.rs"
links = "fake-build_npm_update"
name = "build_npm_update"
version = "0.0.0"
