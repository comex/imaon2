# Autogenerated by gen-cargo-toml (but checked in)
[build-dependencies]
[build-dependencies.build_help]
path = "../build-help"
version = "=0.0.0"

[dependencies]
[dependencies.macros]
path = "../macros"
version = "=0.0.0"

[dependencies.util]
path = "../util"
version = "=0.0.0"

[features]
use_llvm = ["macros/use_llvm", "util/use_llvm", "build_help/use_llvm"]

[lib]
path = "elf_bind.rs"

[package]
build = "build.rs"
links = "fake-fmt_elf_bind"
name = "fmt_elf_bind"
version = "0.0.0"
