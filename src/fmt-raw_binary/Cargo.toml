# Autogenerated by gen-cargo-toml (but checked in)
[build-dependencies]

[dependencies]
[dependencies.util]
path = "../util"
version = "=0.0.0"

[features]
use_llvm = ["util/use_llvm"]

[lib]
path = "raw_binary.rs"

[package]
name = "fmt_raw_binary"
version = "0.0.0"
