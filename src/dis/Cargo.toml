# Autogenerated by gen-cargo-toml (but checked in)
[dependencies]
[dependencies.exec]
path = "../exec"
version = "=0.0.0"

[dependencies.macros]
path = "../macros"
version = "=0.0.0"

[features]
use_llvm = ["macros/use_llvm", "exec/use_llvm"]

[lib]
path = "dis.rs"

[package]
name = "dis"
version = "0.0.0"
